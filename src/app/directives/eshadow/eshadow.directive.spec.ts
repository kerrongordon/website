import { EshadowDirective } from './eshadow.directive';

describe('EshadowDirective', () => {
  it('should create an instance', () => {
    const directive = new EshadowDirective();
    expect(directive).toBeTruthy();
  });
});
