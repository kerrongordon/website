import { SecondaryDirective } from './secondary.directive'

describe('SecondaryDirective', () => {
  it('should create an instance', () => {
    const directive = new SecondaryDirective(null)
    expect(directive).toBeTruthy()
  })
})
