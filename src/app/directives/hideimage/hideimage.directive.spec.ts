import { HideimageDirective } from './hideimage.directive';

describe('HideimageDirective', () => {
  it('should create an instance', () => {
    const directive = new HideimageDirective();
    expect(directive).toBeTruthy();
  });
});
